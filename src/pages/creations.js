import React from 'react'

import Layout from "../components/layout"
import SEO from "../components/seo"

const Creations = () => (
    <Layout>
      <SEO title="Mes créations" keywords={[`creations`, `sites web`, `applications`, `applis`, `scenarios`, `campagne`]} />
      <h1>Mes créations</h1>
      <p>Vous trouverez dans cette partie toutes mes créations, que ce soit dans le developpement ou l'écriture.</p>
    </Layout>
  )

export default Creations;
